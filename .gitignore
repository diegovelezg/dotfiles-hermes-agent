# --- Runtime / state (regenerated by Hermes, not versionable) ---
*.db
*.db-shm
*.db-wal
*.lock
*.bak.*
auth.*
gateway.sock
gateway_state.json
interrupt_debug.log
install_id
state.db*
desktop-build-stamp.json
channel_directory.json

# --- Caches ---
cache/
audio_cache/
image_cache/
images/
models_dev_cache.json
models_dev_cache.etag
provider_models_cache.json
ollama_cloud_models_cache.json
*.skills_prompt_snapshot.json
.bundled_manifest

# --- Logs / sessions / runtime data ---
logs/
sessions/
runtime/
home/
hooks/
pairing/
pets/
skins/
workspace/
desktop/
projects.db
mcp-discovery.lock
update_check

# --- Cron runtime (tickers, executions, output) ---
cron/executions.db
cron/ticker_heartbeat
cron/ticker_last_success
cron/output/

# --- Secrets (use .env.example instead) ---
.env
