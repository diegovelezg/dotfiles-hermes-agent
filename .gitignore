__pycache__/

# Secrets (NEVER commit)
.env
auth.json

# Runtime state (Hermes-managed, regenerated on restart)
gateway.lock
gateway.pid
state.db
state.db-shm
state.db-wal
hermes_state.db

# Large runtime data (generated, not config)
audio_cache/
image_cache/
cache/
logs/
sessions/

# Platform-specific state
whatsapp/session/
sandboxes/
feishu_seen_message_ids.json

# Local dev/build artifacts
node_modules/
.pytest_cache/
